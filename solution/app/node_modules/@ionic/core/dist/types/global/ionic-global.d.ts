import type { IonicConfig, Mode } from '@ionic/core';
export declare const getIonMode: (ref?: any) => Mode;
export declare const initialize: (userConfig?: IonicConfig) => void;
export default initialize;
