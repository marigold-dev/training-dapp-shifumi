import { Origin } from '@airgap/beacon-types';
import { Notification } from '@airgap/beacon-types/dist/esm/types/Notification';
import { PermissionEntity } from '@airgap/beacon-types/dist/esm/types/PermissionEntity';
export declare type AccountIdentifier = string;
export interface AccountInfo extends PermissionEntity {
    accountIdentifier: AccountIdentifier;
    senderId: string;
    origin: {
        type: Origin;
        id: string;
    };
    walletKey?: string;
    publicKey: string;
    connectedAt: number;
    notification?: Notification;
}
