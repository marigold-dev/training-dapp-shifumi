import { AppMetadata, BeaconBaseMessage, BeaconMessageType, Network, PermissionScope, Threshold } from '@airgap/beacon-types';
import { Notification } from '@airgap/beacon-types/dist/esm/types/Notification';
/**
 * @category Message
 */
export interface PermissionResponse extends BeaconBaseMessage {
    type: BeaconMessageType.PermissionResponse;
    appMetadata: AppMetadata;
    publicKey: string;
    network: Network;
    scopes: PermissionScope[];
    threshold?: Threshold;
    notification?: Notification;
}
