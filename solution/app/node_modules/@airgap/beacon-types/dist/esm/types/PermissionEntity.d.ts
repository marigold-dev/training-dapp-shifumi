import { Network, PermissionScope, Threshold } from '@airgap/beacon-types';
export interface PermissionEntity {
    address: string;
    network: Network;
    scopes: PermissionScope[];
    threshold?: Threshold;
}
