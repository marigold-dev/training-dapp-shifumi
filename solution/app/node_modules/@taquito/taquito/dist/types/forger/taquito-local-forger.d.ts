import { ForgeParams, ForgeResponse, Forger } from '@taquito/local-forging';
import { Context } from '@taquito/taquito/dist/types/context';
export declare class TaquitoLocalForger implements Forger {
    private context;
    constructor(context: Context);
    private getNextProto;
    forge({ branch, contents }: ForgeParams): Promise<ForgeResponse>;
}
