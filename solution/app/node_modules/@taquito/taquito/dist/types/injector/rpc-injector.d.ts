import { Context } from '@taquito/taquito/dist/types/context';
import { Injector } from '@taquito/taquito/dist/types/injector/interface';
export declare class RpcInjector implements Injector {
    private context;
    constructor(context: Context);
    inject(signedOperationBytes: string): Promise<string>;
}
