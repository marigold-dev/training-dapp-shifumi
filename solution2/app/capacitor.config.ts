import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "dev.marigold.shifumi2",
  appName: "shifumi2",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
